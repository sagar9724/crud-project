<!DOCTYPE html>
<html lang="en">

<head>
    {{>header}}
</head>

<body>
    <div class="container d-flex flex-column vh-100">

        {{#if user}}
        {{>userNavbar}}
        {{else}}
        {{>navbar}}
        {{/if}}

        <div class="d-flex justify-content-center align-items-center row w-100 h-100" style="flex:1;">
            <div>
                <h1 class="text-center">Riddle</h1>
            </div>
            <div class=" border w-75 h-75" style="">
                <div class="d-flex">
                    <h2 class="m-4">Q1</h2>
                    <h2 class="m-4">What has to be broken before you can use it?</h2>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-sm">
                {{!-- Footer Start --}}
                <div class="card text-center">
                    <div class="card-header">
                        All Rights &copy; 20-21 | Test Skills
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{>footer}}
</body>

</html>